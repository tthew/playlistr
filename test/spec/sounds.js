define([

  // import the module to be tested
  'collections/sounds-collection'

], function(Sounds) {
  'use strict';

  describe("Sounds", function() {
    it("1 == 1", function() {
      expect(1).to.equal(1);
    });

  });

});