define([

  // import the module to be tested
  'collections/playlists-collection'

], function(Playlists) {
  'use strict';

  describe("app", function() {
    it("1 == 1", function() {
      expect(1).to.equal(1);
    });

  });

});