define([

  // import the module to be tested
  // 'app'

], function(app) {
  'use strict';

  describe("Application", function() {
    it("1 == 1", function() {
      expect(1).to.equal(1);
    });

  });

});